import React from "react"


export default ({ tag }) => (
    <section className="tag">
        <div className="animal__name">
                { tag.tag }
           
        </div>    
    </section>
)